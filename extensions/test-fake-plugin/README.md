# Test Fake Plugin

Phase 2 reference extension. Detects `cakephp-agent/fake-plugin` and contributes a rule + skill so installer/resolution tests can prove the extension platform without depending on FriendsOfCake CRUD yet.
