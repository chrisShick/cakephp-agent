# friendsofcake-crud-authorization

Integration pack enabled when **both** `friendsofcake-crud` and `cakephp-authorization` packs are active.

Focus: CRUD find/edit/delete must still authorize and scope — CRUD is not a permission system.
