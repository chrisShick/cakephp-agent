# CRUD + Search integration

Activates only when **both** `friendsofcake-crud` and `friendsofcake-search` extensions are enabled.

Teaches composing CRUD index/query lifecycle with Search filters without copying either pack’s core rules into the other.
