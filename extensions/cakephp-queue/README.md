# cakephp-queue

Capability pack for **`cakephp/queue`** `^2` on CakePHP 5.

If the package is absent, do not invent Laravel queues/`ShouldQueue`/`artisan queue` — prefer Commands/cron or install `cakephp/queue` explicitly.
