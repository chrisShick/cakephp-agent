# cakephp-bake

Light capability pack for **`cakephp/bake`** `^3` on CakePHP 5.

Bake is a generator — output must still pass CakePHP ownership review (`inspect-before-coding`, thin controllers, Table rules/finders).
