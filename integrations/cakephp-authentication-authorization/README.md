# Authentication + Authorization integration

Activates only when **both** `cakephp-authentication` and `cakephp-authorization` extensions are enabled.

Teaches how identity from Authentication feeds Authorization checks without duplicating either pack’s core rules.
